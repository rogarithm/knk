#include <stdio.h>
#include <string.h>

int main(void)
{
	char str[10];

	//*str = 0;
	//putchar(str[0]);

	// str[0] = '\0';
	// putchar(str[0]);

	// strcpy(str, "");
	// putchar(str[0]);

	// strcat(str, "");
	// putchar(str[0]);
}

