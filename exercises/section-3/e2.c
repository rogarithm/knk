#include <stdio.h>

int main(void)
{
		printf("exercise 2. write calls of printf that display a float variable x in the following formats.");

		printf("(a): '%-8.1e'");
		printf("(b): '%10.6e'");
		printf("(c): '%-8.3f'");
		printf("(d): '%6f'");

		return (0);
}
