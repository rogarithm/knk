A function parameter is a ◊var that will be initialized later when the function is called.

There's several restrictions for function's return type. As one, functions may not return arrays. ◊?{Then is it possible to modify elements of an array?

We don't put brackets after an array name for parameter when calling a function that has array parameter.

◊call-by-ref?{A function can change the elements of an array parameter, and ◊uc{we can use the modified value outside the execution.}}
